init_config:
instances:
    - url: "unix://var/run/docker.sock"
      include: []
      exclude:
        - "docker_image:weaveworks/weave"
        - "docker_image:dockercloud/cleanup"
        - "docker_image:dockercloud/logrotate"
        - "docker_image:dockercloud/events"
        - "docker_image:dockercloud/network-daemon"
        - "docker_image:dockercloud/ntpd"